import { Document, Page } from 'react-pdf';

function Resume() {
  const pdfURL = './public/files/Resume_file.pdf';
  return (
    <div>
      {/* <Document file={pdfURL}>
        <Page pageNumber={1} />
      </Document> */}
      hello
    </div>
  );
}
export default Resume;
